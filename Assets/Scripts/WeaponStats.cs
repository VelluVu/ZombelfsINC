using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeaponStats : MonoBehaviour {
    
    /*//weaponstats
    public Stat forceZ;
    public Stat forceY;
    public Stat projectileRotationSpeed;
    public Stat rangedDamage;
    public Stat projectileLifeTime;
    public Stat shotInterval;
    public Stat meleeDamage;
    public Stat areaDamage;
    public Stat spellCost;
    public Stat projectileAreaRadius;
    public Stat critChange;

    private void Start()
    {
        EquipmentManager.instance.onEquipmentChanged += OnEquipmentChanged;
    }

    void OnEquipmentChanged(Equipment newItem, Equipment oldItem)
    {

        if (newItem != null)
        {
            forceY.AddModifier(newItem.projectileSpeedy);
            forceZ.AddModifier(newItem.projectileSpeedz);
            rangedDamage.AddModifier(newItem.projectileDamage);
            projectileRotationSpeed.AddModifier(newItem.projectileRotationSpeed);
            projectileLifeTime.AddModifier(newItem.projectileLifeTime);
            shotInterval.AddModifier(newItem.shotInterval);
            meleeDamage.AddModifier(newItem.meleeDamage);
            projectileAreaRadius.AddModifier(newItem.projectileAreaRadius);
            areaDamage.AddModifier(newItem.projectileAreaDamage);
            spellCost.AddModifier(newItem.spellCost);
            critChange.AddModifier(newItem.criticalChance);

        }

        if (oldItem != null)
        {
            forceY.RemoveModifier(oldItem.projectileSpeedy);
            forceZ.RemoveModifier(oldItem.projectileSpeedz);
            rangedDamage.RemoveModifier(oldItem.projectileDamage);
            projectileRotationSpeed.RemoveModifier(oldItem.projectileRotationSpeed);
            projectileLifeTime.RemoveModifier(oldItem.projectileLifeTime);
            shotInterval.RemoveModifier(oldItem.shotInterval);
            meleeDamage.RemoveModifier(oldItem.meleeDamage);
            projectileAreaRadius.RemoveModifier(oldItem.projectileAreaRadius);
            areaDamage.RemoveModifier(oldItem.projectileAreaDamage);
            spellCost.RemoveModifier(oldItem.spellCost);
            critChange.RemoveModifier(oldItem.criticalChance);

        }
    }*/
}
